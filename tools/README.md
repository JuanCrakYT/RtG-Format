# Tools

This folder should contain decoder/encoder utilities and converters. Do not add tools that assume unverified parts of the format.

## Status

Here is shown a tree of the tools marked as **COMPLETED**, **INCOMPLETE**, **POSTPONED**. These tools will be grouped by folders.

```tree
tools/
└── agent/
     ├── parts_diff.py — **INCOMPLETE** and **POSTPONED**
     └── regenerate_parts.py — **INCOMPLETE** and **POSTPONED**
```