# Building system

This document ties the format to the construction model: Parts, IDs, Properties, Connections and Attachments.

Use `format/*` and `blocks/*` as authoritative sources for how data maps to in-game behaviour; only include confirmed mappings.
