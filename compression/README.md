# Compression research

This folder contains experimental notes about serialization, encoding and compression layers that may be applied to RtG saves.

Important
- Keep compression research separate from `format/` (structured JSON observations). Mark all algorithms as `HYPOTHESIS` until validated.
